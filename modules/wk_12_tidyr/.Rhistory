sizes_and_intensities_cleaned = sizes_and_intensities %>%
select(-c(starts_with("Sizes"))) %>% #Removes repetitive size data
pivot_longer(cols = -1, names_to = "size_nm", values_to = "intensity_percent") #Pivots table so that size values and intensities are in separate columns
sizes_and_intensities_cleaned$size_nm = as.numeric(sizes_and_intensities_cleaned$size_nm)
sizes_and_intensities_cleaned$intensity_percent = as.numeric(sizes_and_intensities_cleaned$intensity_percent)
dls_protein_corona_size_concentration = dls_protein_corona_size_cleaned %>%
select(-c(starts_with("Intensities"), starts_with("Sizes"), starts_with("Volumes"))) %>%
mutate(concentration = str_extract(`Sample Name`, "(?<=_)\\d+\\.?\\d*(?=mg/mL)") %>% as.numeric()) %>%
drop_na(`concentration`) %>%
mutate(elapsed_time = `measurement_time_numeric` - `measurement_time_numeric`[1])
sizes_and_intensities_cleaned %>%
#filter(`Sample Name` == "SeNP_only_15min 1") %>%
ggplot(aes(x=log(size_nm), y=intensity_percent)) +
geom_point() +
facet_wrap(~ `Sample Name`)
dls_protein_corona_size_concentration %>%
ggplot(aes(x=elapsed_time, y=`Z-Average (d.nm)`, color=concentration)) +
geom_point()
dls_protein_corona_size_concentration = dls_protein_corona_size_cleaned %>%
select(-c(starts_with("Intensities"), starts_with("Sizes"), starts_with("Volumes"))) %>%
mutate(concentration = str_extract(`Sample Name`, "(?<=_)\\d+\\.?\\d*(?=mg/mL)") %>% as.factor()) %>%
drop_na(`concentration`) %>%
mutate(elapsed_time = `measurement_time_numeric` - `measurement_time_numeric`[1])
dls_protein_corona_size_concentration %>%
ggplot(aes(x=elapsed_time, y=`Z-Average (d.nm)`, color=concentration)) +
geom_point()
dls_protein_corona_size_concentration %>%
ggplot(aes(x=elapsed_time, y=`Z-Average (d.nm)`, color=concentration)) +
geom_line()
dls_protein_corona_size_concentration %>%
ggplot(aes(x=elapsed_time, y=`Z-Average (d.nm)`, color=concentration)) +
geom_point()
sizes_and_intensities_cleaned %>%
#filter(`Sample Name` == "SeNP_only_15min 1") %>%
ggplot(aes(x=log(size_nm), y=intensity_percent)) +
geom_point() +
facet_wrap(~ `Sample Name`)
dls_protein_corona_size_concentration = dls_protein_corona_size_cleaned %>%
select(-c(starts_with("Intensities"), starts_with("Sizes"), starts_with("Volumes"))) %>% #Creates a new dataframe specifically for average size (and associated PDI values) versus concentration or time
separate(`Sample Name`, into = c("sample_info", "replicate"), sep = " ") %>%
mutate(
replicate = as.integer(replicate), #Extracts replicate information and adds it to the new column "replicate"
concentration = str_extract(sample_info, "\\d+\\.?\\d*") %>% as.numeric() #Extracts NP concentration in mg/mL and adds it to the new column "concentration"
) %>%
drop_na(`concentration`) %>% #Drops all rows with NA in the "concentration" column, as these are control samples
mutate(elapsed_time = `measurement_time_numeric` - `measurement_time_numeric`[1]) #Computes the elapsed time since the first measurement
dls_protein_corona_size_cleaned = dls_protein_corona_size %>%
filter(Type == "Size",) %>% #Removes interdigitated header rows, addresses issue 1 above
select(-c(`S/W Version`, `Serial Number`)) %>% #Removes "S/W Version" and "Serial Number" columns, addresses issue 4 above
mutate(
datetime = as.POSIXct(`Measurement Date and Time`,
format = "%A, %B %d, %Y %I:%M:%S %p"),
measurement_time_numeric = hour(datetime) + minute(datetime)/60 + second(datetime)/3600) %>% #Converts "Measurement Date and Time" column to numeric decimal hours, addresses issue 5 above
select(-c(`Measurement Date and Time`)) #Removes "Measurement Date and Time" column
#load a dataset
package_list <- c("dplyr", "readr", "tidyverse", "tidyr", "lubridate", "dedupewider")
for (pckg in package_list) {
if (!requireNamespace(pckg, quietly = TRUE)) {
install.packages(pckg, dependencies = TRUE)
}
library(pckg, character.only = TRUE)
}
dls_protein_corona_size = read_csv("20250502.csv")
#output the first 10 rows of your data
head(dls_protein_corona_size, 10)
dls_protein_corona_size_cleaned = dls_protein_corona_size %>%
filter(Type == "Size",) %>% #Removes interdigitated header rows, addresses issue 1 above
select(-c(`S/W Version`, `Serial Number`)) %>% #Removes "S/W Version" and "Serial Number" columns, addresses issue 4 above
mutate(
datetime = as.POSIXct(`Measurement Date and Time`,
format = "%A, %B %d, %Y %I:%M:%S %p"),
measurement_time_numeric = hour(datetime) + minute(datetime)/60 + second(datetime)/3600) %>% #Converts "Measurement Date and Time" column to numeric decimal hours, addresses issue 5 above
select(-c(`Measurement Date and Time`)) #Removes "Measurement Date and Time" column
sizes_and_intensities = dls_protein_corona_size_cleaned %>%
select(c(`Sample Name`, starts_with("Sizes"), starts_with("Intensities"))) #Creates a new dataframe specifically for intensity versus size data
colnames(sizes_and_intensities)[72:141] = sizes_and_intensities[1, 2:71] #Renames column headers of intensity data with size values from row 1, addresses issue 2 above
sizes_and_intensities_cleaned = sizes_and_intensities %>%
select(-c(starts_with("Sizes"))) %>% #Removes repetitive size data
pivot_longer(cols = -1, names_to = "size_nm", values_to = "intensity_percent") #Pivots table so that size values and intensities are in separate columns, addresses issue 2 above
sizes_and_intensities_cleaned$size_nm = as.numeric(sizes_and_intensities_cleaned$size_nm) #Converts size values to numeric format
sizes_and_intensities_cleaned$intensity_percent = as.numeric(sizes_and_intensities_cleaned$intensity_percent) #Converts intensity values to numeric format
dls_protein_corona_size_concentration = dls_protein_corona_size_cleaned %>%
select(-c(starts_with("Intensities"), starts_with("Sizes"), starts_with("Volumes"))) %>% #Creates a new dataframe specifically for average size (and associated PDI values) versus concentration or time
separate(`Sample Name`, into = c("sample_info", "replicate"), sep = " ") %>%
mutate(
replicate = as.integer(replicate), #Extracts replicate information and adds it to the new column "replicate"
concentration = str_extract(sample_info, "\\d+\\.?\\d*") %>% as.numeric() #Extracts NP concentration in mg/mL and adds it to the new column "concentration"
) %>%
drop_na(`concentration`) %>% #Drops all rows with NA in the "concentration" column, as these are control samples
mutate(elapsed_time = `measurement_time_numeric` - `measurement_time_numeric`[1]) #Computes the elapsed time since the first measurement
dls_protein_corona_size_cleaned = dls_protein_corona_size %>%
filter(Type == "Size",) %>% #Removes interdigitated header rows, addresses issue 1 above
select(-c(`S/W Version`, `Serial Number`)) %>% #Removes "S/W Version" and "Serial Number" columns, addresses issue 4 above
mutate(
datetime = as.POSIXct(`Measurement Date and Time`,
format = "%A, %B %d, %Y %I:%M:%S %p"),
measurement_time_numeric = hour(datetime) + minute(datetime)/60 + second(datetime)/3600) %>% #Converts "Measurement Date and Time" column to numeric decimal hours, addresses issue 5 above
select(-c(`Measurement Date and Time`)) #Removes "Measurement Date and Time" column
sizes_and_intensities = dls_protein_corona_size_cleaned %>%
select(c(`Sample Name`, starts_with("Sizes"), starts_with("Intensities"))) #Creates a new dataframe specifically for intensity versus size data
colnames(sizes_and_intensities)[72:141] = sizes_and_intensities[1, 2:71] #Renames column headers of intensity data with size values from row 1, addresses issue 2 above
sizes_and_intensities_cleaned = sizes_and_intensities %>%
select(-c(starts_with("Sizes"))) %>% #Removes repetitive size data
pivot_longer(cols = -1, names_to = "size_nm", values_to = "intensity_percent") #Pivots table so that size values and intensities are in separate columns, addresses issue 2 above
sizes_and_intensities_cleaned$size_nm = as.numeric(sizes_and_intensities_cleaned$size_nm) #Converts size values to numeric format
sizes_and_intensities_cleaned$intensity_percent = as.numeric(sizes_and_intensities_cleaned$intensity_percent) #Converts intensity values to numeric format
dls_protein_corona_size_concentration = dls_protein_corona_size_cleaned %>%
select(-c(starts_with("Intensities"), starts_with("Sizes"), starts_with("Volumes"))) %>% #Creates a new dataframe specifically for average size (and associated PDI values) versus concentration or time
separate(`Sample Name`, into = c("sample_info", "replicate"), sep = " ") %>%
mutate(
replicate = as.integer(replicate), #Extracts replicate information and adds it to the new column "replicate"
concentration = str_extract(`sample_info`, "(?<=_)\\d+\\.?\\d*(?=mg/mL)") #Extracts NP concentration in mg/mL and adds it to the new column "concentration"
) %>%
drop_na(`concentration`) %>% #Drops all rows with NA in the "concentration" column, as these are control samples
mutate(elapsed_time = `measurement_time_numeric` - `measurement_time_numeric`[1]) #Computes the elapsed time since the first measurement
tinytex::install_tinytex()
dls_protein_corona_size_concentration %>%
ggplot(aes(x=elapsed_time, y=`Z-Average (d.nm)`, color=concentration)) +
geom_point()
dls_protein_corona_size_cleaned = dls_protein_corona_size %>%
filter(Type == "Size",) %>% #Removes interdigitated header rows, addresses issue 1 above
select(-c(`S/W Version`, `Serial Number`)) %>% #Removes "S/W Version" and "Serial Number" columns, addresses issue 4 above
mutate(
datetime = as.POSIXct(`Measurement Date and Time`,
format = "%A, %B %d, %Y %I:%M:%S %p"),
measurement_time_numeric = hour(datetime) + minute(datetime)/60 + second(datetime)/3600) %>% #Converts "Measurement Date and Time" column to numeric decimal hours, addresses issue 5 above
select(-c(`Measurement Date and Time`)) %>% #Removes "Measurement Date and Time" column
mutate(
replicate = as.integer(replicate), #Extracts replicate information and adds it to the new column "replicate"
concentration = str_extract(`sample_info`, "(?<=_)\\d+\\.?\\d*(?=mg/mL)") #Extracts NP concentration in mg/mL and adds it to the new column "concentration", addresses issue 3 above
)
View(dls_protein_corona_size_cleaned)
dls_protein_corona_size_cleaned = dls_protein_corona_size %>%
filter(Type == "Size",) %>% #Removes interdigitated header rows, addresses issue 1 above
select(-c(`S/W Version`, `Serial Number`)) %>% #Removes "S/W Version" and "Serial Number" columns, addresses issue 4 above
mutate(
datetime = as.POSIXct(`Measurement Date and Time`,
format = "%A, %B %d, %Y %I:%M:%S %p"),
measurement_time_numeric = hour(datetime) + minute(datetime)/60 + second(datetime)/3600) %>% #Converts "Measurement Date and Time" column to numeric decimal hours, addresses issue 5 above
select(-c(`Measurement Date and Time`)) %>% #Removes "Measurement Date and Time" column
separate(`Sample Name`, into = c("sample_info", "replicate"), sep = " ") %>%
mutate(
replicate = as.integer(replicate), #Extracts replicate information and adds it to the new column "replicate"
concentration = str_extract(`sample_info`, "(?<=_)\\d+\\.?\\d*(?=mg/mL)") #Extracts NP concentration in mg/mL and adds it to the new column "concentration", addresses issue 3 above
)
dls_protein_corona_size_concentration %>%
group_by(sample_info) %>%
summarise(mean = mean(`Z-Average (d.nm)`),
sd = sd(`Z-Average (d.nm)`),
n = n(),
se = sd/sqrt(n)) %>%
ggplot(aes(x=elapsed_time, y=`Z-Average (d.nm)`, color=concentration)) +
geom_point()
dls_protein_corona_size_cleaned = dls_protein_corona_size %>%
filter(Type == "Size",) %>% #Removes interdigitated header rows, addresses issue 1 above
select(-c(`S/W Version`, `Serial Number`)) %>% #Removes "S/W Version" and "Serial Number" columns, addresses issue 4 above
mutate(
datetime = as.POSIXct(`Measurement Date and Time`,
format = "%A, %B %d, %Y %I:%M:%S %p"),
measurement_time_numeric = hour(datetime) + minute(datetime)/60 + second(datetime)/3600) %>% #Converts "Measurement Date and Time" column to numeric decimal hours, addresses issue 5 above
select(-c(`Measurement Date and Time`)) %>% #Removes "Measurement Date and Time" column
separate(`Sample Name`, into = c("sample_info", "replicate"), sep = " ") %>%
mutate(
replicate = as.integer(replicate), #Extracts replicate information and adds it to the new column "replicate"
concentration = str_extract(`sample_info`, "(?<=_)\\d+\\.?\\d*(?=mg/mL)") #Extracts NP concentration in mg/mL and adds it to the new column "concentration", addresses issue 3 above
)
sizes_and_intensities = dls_protein_corona_size_cleaned %>%
select(c(`Sample Name`, starts_with("Sizes"), starts_with("Intensities"))) #Creates a new dataframe specifically for intensity versus size data
dls_protein_corona_size_cleaned = dls_protein_corona_size %>%
filter(Type == "Size",) %>% #Removes interdigitated header rows, addresses issue 1 above
select(-c(`S/W Version`, `Serial Number`)) %>% #Removes "S/W Version" and "Serial Number" columns, addresses issue 4 above
mutate(
datetime = as.POSIXct(`Measurement Date and Time`,
format = "%A, %B %d, %Y %I:%M:%S %p"),
measurement_time_numeric = hour(datetime) + minute(datetime)/60 + second(datetime)/3600) %>% #Converts "Measurement Date and Time" column to numeric decimal hours, addresses issue 5 above
select(-c(`Measurement Date and Time`)) %>% #Removes "Measurement Date and Time" column
separate(`Sample Name`, into = c("sample_info", "replicate"), sep = " ") %>%
mutate(
replicate = as.integer(replicate), #Extracts replicate information and adds it to the new column "replicate"
concentration = str_extract(`sample_info`, "(?<=_)\\d+\\.?\\d*(?=mg/mL)") #Extracts NP concentration in mg/mL and adds it to the new column "concentration", addresses issue 3 above
)
sizes_and_intensities = dls_protein_corona_size_cleaned %>%
select(c(`sample_info`, starts_with("Sizes"), starts_with("Intensities"))) #Creates a new dataframe specifically for intensity versus size data
colnames(sizes_and_intensities)[72:141] = sizes_and_intensities[1, 2:71] #Renames column headers of intensity data with size values from row 1, addresses issue 2 above
sizes_and_intensities_cleaned = sizes_and_intensities %>%
select(-c(starts_with("Sizes"))) %>% #Removes repetitive size data
pivot_longer(cols = -1, names_to = "size_nm", values_to = "intensity_percent") #Pivots table so that size values and intensities are in separate columns, addresses issue 2 above
sizes_and_intensities_cleaned$size_nm = as.numeric(sizes_and_intensities_cleaned$size_nm) #Converts size (nm) values to numeric format
sizes_and_intensities_cleaned$intensity_percent = as.numeric(sizes_and_intensities_cleaned$intensity_percent) #Converts intensity (percent) values to numeric format
dls_protein_corona_size_concentration = dls_protein_corona_size_cleaned %>%
select(-c(starts_with("Intensities"), starts_with("Sizes"), starts_with("Volumes"))) %>% #Creates a new dataframe specifically for average size (and associated PDI values) versus concentration or time
drop_na(`concentration`) %>% #Drops all rows with NA in the "concentration" column, as these are control samples
mutate(elapsed_time = `measurement_time_numeric` - `measurement_time_numeric`[1]) #Computes the elapsed time since the first measurement
dls_protein_corona_size_concentration %>%
group_by(sample_info) %>%
summarise(mean = mean(`Z-Average (d.nm)`),
sd = sd(`Z-Average (d.nm)`),
n = n(),
se = sd/sqrt(n)) %>%
ggplot(aes(x=elapsed_time, y=`Z-Average (d.nm)`, color=concentration)) +
geom_point()
sizes_and_intensities_cleaned %>%
#filter(`Sample Name` == "SeNP_only_15min 1") %>%
ggplot(aes(x=log(size_nm), y=intensity_percent)) +
geom_point() +
facet_wrap(~ `Sample Name`)
sizes_and_intensities_cleaned %>%
#filter(`Sample Name` == "SeNP_only_15min 1") %>%
ggplot(aes(x=log(size_nm), y=intensity_percent)) +
geom_point() +
facet_wrap(~ sample_info)
str(dls_protein_corona_size_concentration)
dls_protein_corona_size_concentration$`Z-Average (d.nm)` = as.numeric(dls_protein_corona_size_concentration$`Z-Average (d.nm)`)
dls_protein_corona_size_concentration$PdI = as.numeric(dls_protein_corona_size_concentration$PdI)
dls_protein_corona_size_concentration$concentration = as.factor(dls_protein_corona_size_concentration$concentration)
str(dls_protein_corona_size_concentration)
dls_protein_corona_size_concentration %>%
group_by(sample_info) %>%
summarise(mean = mean(`Z-Average (d.nm)`),
sd = sd(`Z-Average (d.nm)`),
n = n(),
se = sd/sqrt(n)) %>%
ggplot(aes(x=elapsed_time, y=`Z-Average (d.nm)`, color=concentration)) +
geom_point()
str(dls_protein_corona_size_concentration)
dls_protein_corona_size_concentration %>%
group_by(sample_info) %>%
# summarise(mean = mean(`Z-Average (d.nm)`),
#   sd = sd(`Z-Average (d.nm)`),
#   n = n(),
#   se = sd/sqrt(n)) %>%
ggplot(aes(x=elapsed_time, y=`Z-Average (d.nm)`, color=concentration)) +
geom_point()
dls_protein_corona_size_concentration %>%
group_by(sample_info) %>%
summarise(mean = mean(`Z-Average (d.nm)`),
sd = sd(`Z-Average (d.nm)`),
n = n(),
se = sd/sqrt(n)) %>%
ggplot(aes(x=elapsed_time, y=`Z-Average (d.nm)`, color=concentration)) +
geom_point()
sizes_and_intensities_cleaned %>%
#filter(`Sample Name` == "SeNP_only_15min 1") %>%
ggplot(aes(x=log(size_nm), y=intensity_percent)) +
geom_point() +
facet_wrap(~ sample_info)
dls_protein_corona_size_concentration %>%
group_by(sample_info) %>%
summarise(mean_size = mean(`Z-Average (d.nm)`),
mean_time = mean(elapsed_time),
sd_size = sd(`Z-Average (d.nm)`),
sd_time = sd(elapsed_time),
n = n(),
se_size = sd_size/sqrt(n),
se_time = sd_time/sqrt(n)) %>%
ggplot(aes(x=mean_time, y=mean_size, color=concentration)) +
geom_point()
dls_protein_corona_size_cleaned = dls_protein_corona_size %>%
filter(Type == "Size",) %>% #Removes interdigitated header rows, addresses issue 1 above
select(-c(`S/W Version`, `Serial Number`)) %>% #Removes "S/W Version" and "Serial Number" columns, addresses issue 4 above
mutate(
datetime = as.POSIXct(`Measurement Date and Time`,
format = "%A, %B %d, %Y %I:%M:%S %p"),
measurement_time_numeric = hour(datetime) + minute(datetime)/60 + second(datetime)/3600) %>% #Converts "Measurement Date and Time" column to numeric decimal hours, addresses issue 5 above
select(-c(`Measurement Date and Time`)) %>% #Removes "Measurement Date and Time" column
separate(`Sample Name`, into = c("sample_info", "replicate"), sep = " ") %>%
mutate(
replicate = as.integer(replicate), #Extracts replicate information and adds it to the new column "replicate"
concentration = str_extract(`sample_info`, "(?<=_)\\d+\\.?\\d*(?=mg/mL)") #Extracts NP concentration in mg/mL and adds it to the new column "concentration", addresses issue 3 above
)
sizes_and_intensities = dls_protein_corona_size_cleaned %>%
select(c(`sample_info`, starts_with("Sizes"), starts_with("Intensities"))) #Creates a new dataframe specifically for intensity versus size data
colnames(sizes_and_intensities)[72:141] = sizes_and_intensities[1, 2:71] #Renames column headers of intensity data with size values from row 1, addresses issue 2 above
sizes_and_intensities_cleaned = sizes_and_intensities %>%
select(-c(starts_with("Sizes"))) %>% #Removes repetitive size data
pivot_longer(cols = -1, names_to = "size_nm", values_to = "intensity_percent") #Pivots table so that size values and intensities are in separate columns, addresses issue 2 above
sizes_and_intensities_cleaned$size_nm = as.numeric(sizes_and_intensities_cleaned$size_nm) #Converts "size_nm" values to numeric format
sizes_and_intensities_cleaned$intensity_percent = as.numeric(sizes_and_intensities_cleaned$intensity_percent) #Converts "intensity_percent" values to numeric format
dls_protein_corona_size_concentration = dls_protein_corona_size_cleaned %>%
select(-c(starts_with("Intensities"), starts_with("Sizes"), starts_with("Volumes"))) %>% #Creates a new dataframe specifically for average size (and associated PDI values) versus concentration or time
drop_na(`concentration`) %>% #Drops all rows with NA in the "concentration" column, as these are control samples
mutate(elapsed_time = `measurement_time_numeric` - `measurement_time_numeric`[1]) #Computes the elapsed time since the first measurement
dls_protein_corona_size_concentration$`Z-Average (d.nm)` = as.numeric(dls_protein_corona_size_concentration$`Z-Average (d.nm)`) #Converts "Z-Average (d.nm)" values to numeric format
dls_protein_corona_size_concentration$PdI = as.numeric(dls_protein_corona_size_concentration$PdI) #Converts "PdI" values to numeric format
dls_protein_corona_size_concentration$concentration = as.factor(dls_protein_corona_size_concentration$concentration) #Converts "concentration" values to factors
dls_protein_corona_size_concentration %>%
group_by(sample_info) %>%
summarise(mean_size = mean(`Z-Average (d.nm)`),
mean_time = mean(elapsed_time),
sd_size = sd(`Z-Average (d.nm)`),
sd_time = sd(elapsed_time),
n = n(),
se_size = sd_size/sqrt(n),
se_time = sd_time/sqrt(n)) %>%
ggplot(aes(x=mean_time, y=mean_size, color=concentration)) +
geom_point()
dls_protein_corona_size_concentration %>%
group_by(sample_info) %>%
summarise(mean_size = mean(`Z-Average (d.nm)`),
mean_time = mean(elapsed_time),
sd_size = sd(`Z-Average (d.nm)`),
sd_time = sd(elapsed_time),
n = n(),
se_size = sd_size/sqrt(n),
se_time = sd_time/sqrt(n)) %>%
ggplot(aes(x=mean_time, y=mean_size)) +
geom_point()
dls_protein_corona_size_concentration %>%
group_by(sample_info) %>%
summarise(mean_size = mean(`Z-Average (d.nm)`),
mean_time = mean(elapsed_time),
sd_size = sd(`Z-Average (d.nm)`),
sd_time = sd(elapsed_time),
n = n(),
se_size = sd_size/sqrt(n),
se_time = sd_time/sqrt(n),
concentration = concentration) %>%
ggplot(aes(x=mean_time, y=mean_size), color = concentration) +
geom_point()
dls_protein_corona_size_concentration$concentration = as.numeric(dls_protein_corona_size_concentration$concentration) #Converts "concentration" values to factors
dls_protein_corona_size_concentration %>%
group_by(sample_info) %>%
summarise(mean_size = mean(`Z-Average (d.nm)`),
mean_time = mean(elapsed_time),
sd_size = sd(`Z-Average (d.nm)`),
sd_time = sd(elapsed_time),
n = n(),
se_size = sd_size/sqrt(n),
se_time = sd_time/sqrt(n),
concentration = mean(concentration)) %>%
ggplot(aes(x=mean_time, y=mean_size), color = concentration) +
geom_point()
dls_protein_corona_size_concentration %>%
group_by(sample_info) %>%
summarise(mean_size = mean(`Z-Average (d.nm)`),
mean_time = mean(elapsed_time),
sd_size = sd(`Z-Average (d.nm)`),
sd_time = sd(elapsed_time),
n = n(),
se_size = sd_size/sqrt(n),
se_time = sd_time/sqrt(n),
mean_concentration = mean(concentration)) %>%
ggplot(aes(x=mean_time, y=mean_size), color = mean_concentration) +
geom_point()
dls_protein_corona_size_concentration %>%
group_by(sample_info) %>%
summarise(mean_size = mean(`Z-Average (d.nm)`),
mean_time = mean(elapsed_time),
sd_size = sd(`Z-Average (d.nm)`),
sd_time = sd(elapsed_time),
n = n(),
se_size = sd_size/sqrt(n),
se_time = sd_time/sqrt(n),
mean_concentration = mean(concentration)) %>%
ggplot(aes(x=mean_time, y=mean_size, color = mean_concentration)) +
geom_point()
dls_protein_corona_size_concentration %>%
group_by(sample_info) %>%
summarise(mean_size = mean(`Z-Average (d.nm)`),
mean_time = mean(elapsed_time),
sd_size = sd(`Z-Average (d.nm)`),
sd_time = sd(elapsed_time),
n = n(),
se_size = sd_size/sqrt(n),
se_time = sd_time/sqrt(n),
mean_concentration = mean(concentration)) %>%
concentration = as.factor(mean_concentration) %>%
ggplot(aes(x=mean_time, y=mean_size, color = mean_concentration)) +
geom_point()
dls_protein_corona_size_concentration %>%
group_by(sample_info) %>%
summarise(mean_size = mean(`Z-Average (d.nm)`),
mean_time = mean(elapsed_time),
sd_size = sd(`Z-Average (d.nm)`),
sd_time = sd(elapsed_time),
n = n(),
se_size = sd_size/sqrt(n),
se_time = sd_time/sqrt(n),
concentration = mean(concentration)) %>%
concentration = as.factor(concentration) %>%
ggplot(aes(x=mean_time, y=mean_size, color = mean_concentration)) +
geom_point()
dls_protein_corona_size_concentration %>%
group_by(sample_info) %>%
summarise(mean_size = mean(`Z-Average (d.nm)`),
mean_time = mean(elapsed_time),
sd_size = sd(`Z-Average (d.nm)`),
sd_time = sd(elapsed_time),
n = n(),
se_size = sd_size/sqrt(n),
se_time = sd_time/sqrt(n),
mean_concentration = mean(concentration)) %>%
#concentration = as.factor(concentration) %>%
ggplot(aes(x=mean_time, y=mean_size, color = mean_concentration)) +
geom_point()
dls_protein_corona_size_concentration %>%
group_by(sample_info, concentration) %>%
summarise(mean_size = mean(`Z-Average (d.nm)`),
mean_time = mean(elapsed_time),
sd_size = sd(`Z-Average (d.nm)`),
sd_time = sd(elapsed_time),
n = n(),
se_size = sd_size/sqrt(n),
se_time = sd_time/sqrt(n)) %>%
ggplot(aes(x=mean_time, y=mean_size, color = concentration)) +
geom_point()
dls_protein_corona_size_concentration$concentration = as.factor(dls_protein_corona_size_concentration$concentration) #Converts "concentration" values to factors
dls_protein_corona_size_concentration %>%
group_by(sample_info, concentration) %>%
summarise(mean_size = mean(`Z-Average (d.nm)`),
mean_time = mean(elapsed_time),
sd_size = sd(`Z-Average (d.nm)`),
sd_time = sd(elapsed_time),
n = n(),
se_size = sd_size/sqrt(n),
se_time = sd_time/sqrt(n)) %>%
ggplot(aes(x=mean_time, y=mean_size, color = concentration)) +
geom_point()
dls_protein_corona_size_concentration %>%
group_by(sample_info, concentration) %>%
summarise(mean_size = mean(`Z-Average (d.nm)`),
mean_time = mean(elapsed_time),
sd_size = sd(`Z-Average (d.nm)`),
sd_time = sd(elapsed_time),
n = n(),
se_size = sd_size/sqrt(n),
se_time = sd_time/sqrt(n)) %>%
ggplot(aes(x=mean_time, y=mean_size, color = concentration)) +
geom_point() +
labs(x = "Elapsed time since initial measurement (hr)",
y = "Mean size (nm)",
title = "Increase in D(H) as a function of incubation time and NP concentration")
dls_protein_corona_size_concentration %>%
group_by(sample_info, concentration) %>%
summarise(mean_size = mean(`Z-Average (d.nm)`),
mean_time = mean(elapsed_time),
sd_size = sd(`Z-Average (d.nm)`),
sd_time = sd(elapsed_time),
n = n(),
se_size = sd_size/sqrt(n),
se_time = sd_time/sqrt(n)) %>%
ggplot(aes(x=mean_time, y=mean_size, color = concentration)) +
geom_point() +
labs(x = "Elapsed time since initial measurement (hr)",
y = "Mean size (nm)",
title = "Increase in D(H) as a function of incubation time and NP concentration") +
theme(plot.title = element_text(hjust = 0.5), plot.subtitle = element_text(hjust = 0.5))
dls_protein_corona_size_concentration %>%
group_by(sample_info, concentration) %>%
summarise(mean_size = mean(`Z-Average (d.nm)`),
mean_time = mean(elapsed_time),
sd_size = sd(`Z-Average (d.nm)`),
sd_time = sd(elapsed_time),
n = n(),
se_size = sd_size/sqrt(n),
se_time = sd_time/sqrt(n)) %>%
ggplot(aes(x=mean_time, y=mean_size, color = concentration)) +
geom_point() +
labs(x = "Elapsed time since initial measurement (hr)",
y = "Mean size (nm)",
title = "Increase in D(H) as a function of incubation time and NP concentration") +
theme(plot.title = element_text(hjust = 0.5), plot.subtitle = element_text(hjust = 0.5)) +
theme_minimal()
dls_protein_corona_size_concentration %>%
group_by(sample_info, concentration) %>%
summarise(mean_size = mean(`Z-Average (d.nm)`),
mean_time = mean(elapsed_time),
sd_size = sd(`Z-Average (d.nm)`),
sd_time = sd(elapsed_time),
n = n(),
se_size = sd_size/sqrt(n),
se_time = sd_time/sqrt(n)) %>%
ggplot(aes(x=mean_time, y=mean_size, color = concentration)) +
geom_point() +
labs(x = "Elapsed time since initial measurement (hr)",
y = "Mean size (nm)",
title = "Increase in D(H) as a function of incubation time and NP concentration") +
theme(plot.title = element_text(hjust = 0.5), plot.subtitle = element_text(hjust = 0.5)) +
theme_linedraw()
dls_protein_corona_size_concentration %>%
group_by(sample_info, concentration) %>%
summarise(mean_size = mean(`Z-Average (d.nm)`),
mean_time = mean(elapsed_time),
sd_size = sd(`Z-Average (d.nm)`),
sd_time = sd(elapsed_time),
n = n(),
se_size = sd_size/sqrt(n),
se_time = sd_time/sqrt(n)) %>%
ggplot(aes(x=mean_time, y=mean_size, color = concentration)) +
geom_point() +
labs(x = "Elapsed time since initial measurement (hr)",
y = "Mean size (nm)",
title = "Increase in D(H) as a function of incubation time and NP concentration") +
theme(plot.title = element_text(hjust = 0.5), plot.subtitle = element_text(hjust = 0.5)) +
theme_classic()
dls_protein_corona_size_concentration %>%
group_by(sample_info, concentration) %>%
summarise(mean_size = mean(`Z-Average (d.nm)`),
mean_time = mean(elapsed_time),
sd_size = sd(`Z-Average (d.nm)`),
sd_time = sd(elapsed_time),
n = n(),
se_size = sd_size/sqrt(n),
se_time = sd_time/sqrt(n)) %>%
ggplot(aes(x=mean_time, y=mean_size, color = concentration)) +
geom_point() +
labs(x = "Elapsed time since initial measurement (hr)",
y = "Mean size (nm)",
title = "Increase in D(H) as a function of incubation time and NP concentration",
color = "Concentration (mg/mL)") +
theme(plot.title = element_text(hjust = 0.5), plot.subtitle = element_text(hjust = 0.5)) +
theme_classic()
